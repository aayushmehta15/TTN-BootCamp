import React from "react";

const AboutUsComp = () => {
    return (
        <div>
            <h1>Description :</h1>
            <p>
                Create a user application which will have a home page, add user
                page and about us page .On Home, It will list all user in a
                table. There will be one more column Action in a table which
                will have a delete option to delete the user. On add New user
                page, there will be form to create a new user. There will be a
                middleware that will add the created_on date in user when new
                user will be created. On About-us page, there will some
                description of app and app creator.
            </p>
        </div>
    );
};

export default AboutUsComp;
